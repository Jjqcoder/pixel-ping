generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  // 使用SQLite数据库
  url      = env("DATABASE_URL")
}

model users {
  ID       Int     @id @default(autoincrement())
  USERNAME String?
  PASSWORD String?
}
