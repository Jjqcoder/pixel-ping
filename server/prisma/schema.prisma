generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  // 为了兼容Prisma 6.x版本保留url属性
  url      = env("DATABASE_URL")
}

model users {
  ID       Int     @id @default(autoincrement())
  USERNAME String? @db.VarChar(255)
  PASSWORD String? @db.VarChar(255)
}
